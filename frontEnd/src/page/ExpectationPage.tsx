import {Button} from "antd";
import {PlusOutlined} from "@ant-design/icons";

const ExpectationPage = () =>{
    return <div>
        <div>
            <Button type={"text"} icon={<PlusOutlined />}>Add Expectation</Button>
        </div>

    </div>
}
export default ExpectationPage;
